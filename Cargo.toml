# `.bsn` importer workspace: the `aurora_bsn` library + per-asset importer binaries and a viewer.
# Converts source OBJ/MTL or glTF into `.cluster_mesh` assets and a `.bsn` scene, sidestepping
# glTF's lack of displacement/depth maps; the viewer renders the result on aurora
# (https://github.com/slyedoc/aurora, crate `bevy_aurora`).
#
# PARKED (excluded): `furnace` / `grader` / `xtask` render on the old wgpu solari stack;
# `g1` needs the `.animclip` transcoder from the old engine's bevy branch; `bake_person` /
# `dressing_room` need make_human, which still path-depends on the old engine.
[workspace]
resolver = "3"
members = [
  "crates/aurora_bsn",
  "crates/util",
  "crates/bsn",
  "crates/bevy_city",
  "importers/bistro",
  "importers/hoverboard",
  "importers/kenney",
  "importers/lunarbase",
  "importers/namaqualand_boulder",
  "importers/polyhaven",
  "importers/prop",
  "importers/san_miguel",
  "importers/speedtree",
  "importers/wow",
  "importers/zeroday",
  "importers/animlib",
  "crates/animgraph_editor",
  "crates/scene_editor",
]
exclude = [
  "crates/furnace",
  "crates/grader",
  "crates/xtask",
  "importers/g1",
  "importers/bake_person",
  "importers/dressing_room",
]

[workspace.dependencies]
# The bevy the aurora engine tracks (upstream main + render-free feathers, the .bsn loader,
# bevy_feathers_inspector): the side-by-side checkout at ../../f/bevy, which sits on branch
# `aurora`. Importers only need the mesh/asset half; the viewer gets the renderer's own feature
# set unioned in through `bevy_aurora`.
bevy = { path = "../../f/bevy", default-features = false, features = [
  "default_app",
  "default_platform",
  "common_api",
  "std",
  "multi_threaded",
  "scene",
  "serialize",
  "bevy_camera",
  "bevy_camera_controller",
  "free_camera",
  "bevy_mesh",
  "bevy_mikktspace",
  "bevy_image",
  "bevy_color",
  "png",
  "jpeg",
  "ktx2",
  "zstd_rust",
] }
# The `.cluster_mesh` wire format + flat bake, without the renderer (importers).
aurora_cluster_mesh = { git = "https://github.com/slyedoc/aurora.git", branch = "main" }
# The engine itself (viewer only).
bevy_aurora = { git = "https://github.com/slyedoc/aurora.git", branch = "main" }
# CLI parsing for the importer binaries.
clap = { version = "4", features = ["derive"] }

[patch."https://github.com/slyedoc/aurora"]
bevy_aurora = { path = "../aurora" }
# The cluster-mesh format crate too: the skinned bake (UAL) needed `from_mesh_flat` to carry
# JOINT_INDEX / JOINT_WEIGHT, which `to_mesh` already read back.
aurora_cluster_mesh = { path = "../aurora/crates/aurora_cluster_mesh" }

# make_human's manifest reaches bevy/avian/enhanced_input by their upstream
# URLs (same convention as zero); redirect them onto the one stacked graph.
[patch."https://github.com/bevyengine/bevy"]
bevy = { path = "../../f/bevy" }
bevy_a11y = { path = "../../f/bevy/crates/bevy_a11y" }
bevy_animation = { path = "../../f/bevy/crates/bevy_animation" }
bevy_app = { path = "../../f/bevy/crates/bevy_app" }
bevy_asset = { path = "../../f/bevy/crates/bevy_asset" }
bevy_camera = { path = "../../f/bevy/crates/bevy_camera" }
bevy_color = { path = "../../f/bevy/crates/bevy_color" }
bevy_derive = { path = "../../f/bevy/crates/bevy_derive" }
bevy_diagnostic = { path = "../../f/bevy/crates/bevy_diagnostic" }
bevy_ecs = { path = "../../f/bevy/crates/bevy_ecs" }
bevy_feathers = { path = "../../f/bevy/crates/bevy_feathers" }
bevy_gilrs = { path = "../../f/bevy/crates/bevy_gilrs" }
bevy_gizmos = { path = "../../f/bevy/crates/bevy_gizmos" }
bevy_gltf = { path = "../../f/bevy/crates/bevy_gltf" }
bevy_image = { path = "../../f/bevy/crates/bevy_image" }
bevy_input = { path = "../../f/bevy/crates/bevy_input" }
bevy_input_focus = { path = "../../f/bevy/crates/bevy_input_focus" }
bevy_internal = { path = "../../f/bevy/crates/bevy_internal" }
bevy_light = { path = "../../f/bevy/crates/bevy_light" }
bevy_log = { path = "../../f/bevy/crates/bevy_log" }
bevy_macro_utils = { path = "../../f/bevy/crates/bevy_macro_utils" }
bevy_material = { path = "../../f/bevy/crates/bevy_material" }
bevy_math = { path = "../../f/bevy/crates/bevy_math" }
bevy_mesh = { path = "../../f/bevy/crates/bevy_mesh" }
bevy_picking = { path = "../../f/bevy/crates/bevy_picking" }
bevy_platform = { path = "../../f/bevy/crates/bevy_platform" }
bevy_ptr = { path = "../../f/bevy/crates/bevy_ptr" }
bevy_reflect = { path = "../../f/bevy/crates/bevy_reflect" }
bevy_scene = { path = "../../f/bevy/crates/bevy_scene" }
bevy_state = { path = "../../f/bevy/crates/bevy_state" }
bevy_tasks = { path = "../../f/bevy/crates/bevy_tasks" }
bevy_text = { path = "../../f/bevy/crates/bevy_text" }
bevy_time = { path = "../../f/bevy/crates/bevy_time" }
bevy_transform = { path = "../../f/bevy/crates/bevy_transform" }
bevy_ui = { path = "../../f/bevy/crates/bevy_ui" }
bevy_ui_widgets = { path = "../../f/bevy/crates/bevy_ui_widgets" }
bevy_utils = { path = "../../f/bevy/crates/bevy_utils" }
bevy_window = { path = "../../f/bevy/crates/bevy_window" }
bevy_winit = { path = "../../f/bevy/crates/bevy_winit" }

[patch."https://github.com/avianphysics/avian"]
avian3d = { git = "https://github.com/slyedoc/avian", branch = "aurora" }

[patch."https://github.com/simgine/bevy_enhanced_input"]
bevy_enhanced_input = { git = "https://github.com/slyedoc/bevy_enhanced_input", branch = "aurora" }
